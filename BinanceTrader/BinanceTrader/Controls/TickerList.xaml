<UserControl x:Class="BinanceTrader.Controls.TickerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BinanceTrader.Controls"
             mc:Ignorable="d" 
             xmlns:m="clr-namespace:BinanceTrader.Localize.Markup"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView Name="_listView" ItemsSource="{Binding Path=Prices}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{m:Loc Symbol}" DisplayMemberBinding="{Binding Symbol}" Width="250"/>
                    <GridViewColumn Header="{m:Loc Price}" DisplayMemberBinding="{Binding Price}" Width="100"/>
                    <GridViewColumn Header="{m:Loc Price}" DisplayMemberBinding="{Binding Price}" Width="100"/>
                    <GridViewColumn Header="{m:Loc Price}" DisplayMemberBinding="{Binding Price}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
